# =============================================================================
# Stage 1: Build
# =============================================================================
FROM --platform=$BUILDPLATFORM docker.io/library/golang:1.25-alpine AS builder

# Install build dependencies
RUN apk add --no-cache git ca-certificates tzdata

WORKDIR /src

# Copy go mod/sum first for better layer caching
COPY go.mod go.sum ./
RUN go mod download

# Copy source
COPY . .

# Build args for cross-compilation (multi-arch support)
ARG TARGETOS
ARG TARGETARCH
ARG VERSION=dev
ARG COMMIT=unknown
ARG BUILDTIME=unknown

# Build otoxan-mcp-memory binary with version ldflags
RUN CGO_ENABLED=0 GOOS=${TARGETOS:-linux} GOARCH=${TARGETARCH:-amd64} \
    go build -trimpath \
    -ldflags "\
      -X github.com/silas/otoxan/internal/version.Version=${VERSION} \
      -X github.com/silas/otoxan/internal/version.Commit=${COMMIT} \
      -X github.com/silas/otoxan/internal/version.BuildTime=${BUILDTIME}" \
    -o /otoxan-mcp-memory \
    ./cmd/otoxan-mcp-memory/

# =============================================================================
# Stage 2: Runtime
# =============================================================================
FROM docker.io/library/alpine:3.21 AS runtime

# Install runtime dependencies
RUN apk add --no-cache ca-certificates tzdata

# Create non-root user for security
RUN addgroup -g 1000 mcp-memory && \
    adduser -u 1000 -G mcp-memory -s /bin/sh -D mcp-memory

WORKDIR /app

# Copy binary from builder
COPY --from=builder /otoxan-mcp-memory /app/otoxan-mcp-memory

# Copy health check wrapper script
COPY --from=builder /src/deploy/docker/healthcheck-mcp.sh /usr/local/bin/healthcheck

# Copy config dir skeleton (can be volume-mounted at runtime)
RUN mkdir -p /etc/otoxan /home/mcp-memory/.otoxan/run && \
    chown -R mcp-memory:mcp-memory /app /home/mcp-memory/.otoxan

USER mcp-memory:mcp-memory

# Expose the standard otoxan home path
ENV OTOXAN_HOME=/home/mcp-memory/.otoxan

# Health check: run a quick initialization test against Mongo
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD /usr/local/bin/healthcheck /app/otoxan-mcp-memory || exit 1

ENTRYPOINT ["/app/otoxan-mcp-memory"]
